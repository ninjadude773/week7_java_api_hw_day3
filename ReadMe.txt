Using  JavaScript(fetch) and the DOM you are to create a table of data using the F1 racer API.

The table should have a total of 7 rows and dynamically populate the data when a

"season" and "round" are specified within your form.

Once completed, commit the code to github and submit the github link to the assignment.